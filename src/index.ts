/**
 * sbom-pilot public API surface (Phase α scaffolding placeholder).
 *
 * The Phase 1 implementation will populate this barrel with the three
 * top-level operations defined by spec.md §10: `generate` (SBOM emit),
 * `scan` (vulnerability correlation), and `report` (compliance reports).
 *
 * For T-01 (foundation), this file exists only so that tsconfig.json's
 * `include` glob has something to match.
 */
export const PACKAGE_NAME = 'sbom-pilot';
